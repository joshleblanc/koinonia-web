<%= simple_form_for conversation_content.persisted? ? conversation_content : [conversation_content.conversation, conversation_content] do %>
  <%= _1.error_notification %>
  <%= _1.input :text %>
  <%= _1.button :submit %>
  <%= _1.error :base %>
<% end %>