<%= render 'shared/hero', title: "New Conversation Content" %>

<%= turbo_frame_tag dom_id(@conversation, :new_content) do %>
  <section class="section">
    <div class="container">
      <%= render "form", conversation_content: @conversation_content %>
    </div>
  </section>

  <br>
<% end %>

