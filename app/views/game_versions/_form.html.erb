<%= simple_form_for game_version.persisted? ? game_version : [game_version.game, game_version] do %>
  <%= _1.input :scripts %>
  <%= _1.input :level %>
  <%= _1.association :package_versions, collection: Package.all, value_method: :latest_package_version_id, label_method: :name, input_html: { data: { tom_select_url_value: packages_url, tom_select_value_field: "latest_package_version_id"} } %>
  <%= _1.button :submit %>
<% end %>