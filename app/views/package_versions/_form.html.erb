<%= simple_form_for package_version.persisted? ? package_version : [package_version.package, package_version] do %>
  <%= _1.input :levels %>
  <%= _1.input :scripts %> 
  <%= _1.input :templates %>
  <%= _1.button :submit %>
<% end %>