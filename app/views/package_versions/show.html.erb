<p style="color: green"><%= notice %></p>
<section class="section">
  <div class="container">
    <%= render "shared/hero", title: @package_version.package.name %>

    <%= turbo_frame_tag dom_id(@package_version.package, :versions) do %>
      <div class="box">
        <%= render @package_version %>
      </div>
    <% end %>
  </div>
</section>